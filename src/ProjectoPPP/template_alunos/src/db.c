#include "db.h"

#include <stdio.h>

int db_carregar(AlunoLista* lista, const char* alunos_path, const char* despesas_path) {
    /* TODO(aluno): abrir ficheiros, parsear linhas e preencher listas em memória. */
    printf("Ainda não implementado: db_carregar\n");
    (void)lista;
    (void)alunos_path;
    (void)despesas_path;
    return 0;
}

int db_guardar(const AlunoLista* lista, const char* alunos_path, const char* despesas_path) {
    /* TODO(aluno): serializar alunos e despesas para ficheiros de texto. */
    printf("Ainda não implementado: db_guardar\n");
    (void)lista;
    (void)alunos_path;
    (void)despesas_path;
    return 0;
}
